@using Okapia.Domain.SeachModels
@using Okapia.Domain.ViewModels.Group
@model IEnumerable<GroupViewModel>
@{
  var searchModel = ViewData["searchModel"] as GroupSearchModel;
}
<div class="row">
  <div class="col-md-12">
    <div class="panel panel-border panel-primary">
      <div class="panel-heading">
        <h2 class="panel-title">لیست گروه های کاربری</h2>
      </div>
      <div class="panel-body">
        <table id="datatable" class="table table-striped table-bordered">
          <thead>
          <tr>
          <th>
            ردیف
          </th>
          <th>
            @Html.DisplayNameFor(model => model.Name)
          </th>
          <th>
            @Html.DisplayNameFor(model => model.Description)
          </th>
          <th>
            @Html.DisplayNameFor(model => model.UsersCount)
          </th>
          <th>
            عملیات
          </th>

          </thead>
          <tbody>
          @foreach (var item in Model.Select((value, index) => new {index, value}))
          {
            <tr>
              <td>
                @{
                  var index = item.index;
                  index += 1;
                }
                @index
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.value.Name)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.value.Description)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.value.UsersCount)
              </td>
              <td>
                <a class="btn btn-sm btn-info" onclick="openEditCityModal('@item.value.Id')">ویرایش</a>
                @if (!item.value.IsDeleted)
                {
                  <a class="btn btn-sm btn-danger" asp-area="Administrator" asp-controller="Group" asp-action="Delete" asp-route-id="@item.value.Id">حذف</a>
                }
                else
                {
                  <a class="btn btn-sm btn-success" asp-area="Administrator" asp-controller="Group" asp-action="Activate" asp-route-id="@item.value.Id">فعال سازی</a>
                }
                <a class="btn btn-sm btn-purple" asp-area="Administrator" asp-controller="Group" asp-action="Clear" asp-route-id="@item.value.Id">حذف اعضا</a>
              </td>
            </tr>
          }
          </tbody>
        </table>
      </div>
      <nav>
        <ul class="pagination">
          @for (var i = 0; i < searchModel.PageCount; i++)
          {
            var routingSearchModel = new GroupSearchModel
            {
              Name = searchModel.Name,
              Description = searchModel.Description,
              IsDeleted = searchModel.IsDeleted,
              PageSize = 80,
              PageIndex = i
            };
            if (searchModel.PageIndex == i)
            {
              <li class="active">
                @Html.ActionLink((i + 1).ToString(), "Index", "Group", routingSearchModel)
              </li>
            }
            else
            {
              <li>
                @Html.ActionLink((i + 1).ToString(), "Index", "Group", routingSearchModel)
              </li>
            }
          }
        </ul>
      </nav>
    </div>
  </div>
</div>

<div id="editGroupDiv">

</div>
<script>
  function openEditCityModal(id) {
    openModalWithData(`@Url.Action("Edit", "Group")/${id}`, 'editGroupDiv', 'editGroupModal');
  }
</script>